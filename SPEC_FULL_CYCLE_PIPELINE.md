# Спецификация: Full-Cycle Pipeline

## Обзор

Автономный пайплайн для полного цикла разработки: от описания задачи до merge в main.

```
Пользователь описывает задачу
        ↓
AI анализирует кодбейз (RAG)
        ↓
AI показывает план → подтверждение пользователя
        ↓
AI вносит изменения в код
        ↓
AI создаёт ветку → коммит → PR
        ↓
AI делает self-review PR
        ↓
AI исправляет замечания (цикл)
        ↓
CI проходит → AI мерджит в main
        ↓
Итоговый отчёт
```

---

## 1. Функциональные требования

### 1.1 Запуск пайплайна

| Способ | Описание | Пример |
|--------|----------|--------|
| Команда | `/full-cycle <описание задачи>` | `/full-cycle добавить кнопку выхода в настройках` |
| Естественный язык | AI распознаёт интент "сделай и залей" | `сделай фичу X и залей в main` |

**Intent classification:**
- Если сообщение содержит паттерны типа "сделай и залей", "реализуй и смержи", "добавь и закоммить" → запустить full-cycle
- Иначе → обычный чат

### 1.2 Этапы пайплайна

#### Этап 1: Анализ задачи
- Прочитать описание задачи от пользователя
- Использовать RAG для поиска релевантных файлов
- Определить какие файлы нужно изменить

#### Этап 2: Показ плана (с подтверждением)
```
📋 План изменений:

1. Изменить src/main/kotlin/App.kt
   - Добавить новую функцию logout()

2. Изменить src/main/kotlin/ui/SettingsScreen.kt
   - Добавить кнопку "Выход"
   - Подключить обработчик logout()

3. Создать тест tests/LogoutTest.kt

Начать выполнение? [y/n]
```

Пользователь должен подтвердить (y) или отменить (n).

#### Этап 3: Внесение изменений
- AI вносит изменения в файлы локально
- Использует существующие возможности редактирования (File Storage MCP или прямое редактирование)

#### Этап 4: Создание PR
1. Создать новую ветку: `feature/ai-<краткое-описание>-<timestamp>`
2. Сделать коммит с описательным сообщением
3. Запушить ветку на GitHub
4. Создать Pull Request

#### Этап 5: Self-Review
- AI ревьюит свой же PR используя существующий `PrReviewService`
- Оставляет комментарии к проблемным местам

#### Этап 6: Исправление замечаний (цикл)
```
while (есть замечания review) {
    исправить замечания
    сделать коммит
    запустить self-review снова

    if (итерация > 10) {
        остановиться и спросить пользователя
        break
    }
}
```

#### Этап 7: Ожидание CI
- Дождаться завершения GitHub Actions CI
- Если CI упал:
  - Прочитать логи CI
  - Понять ошибку
  - Исправить и запушить снова
  - Повторить

#### Этап 8: Merge
- Только если CI прошёл успешно
- Merge PR в main
- Удалить feature-ветку

#### Этап 9: Итоговый отчёт
```
✅ Задача выполнена!

PR: #123 (https://github.com/user/repo/pull/123)
Ветка: feature/ai-logout-button-1705123456

Изменённые файлы:
- src/main/kotlin/App.kt (+15, -2)
- src/main/kotlin/ui/SettingsScreen.kt (+42, -5)
- tests/LogoutTest.kt (новый файл, +28)

Статистика:
- Итераций review: 3
- CI запусков: 2

Описание изменений:
Добавлена кнопка "Выход" в экран настроек. При нажатии
вызывается функция logout(), которая очищает сессию и
возвращает на экран входа.
```

---

## 2. Обработка ошибок и edge cases

### 2.1 CI упал
```kotlin
when (ciStatus) {
    FAILED -> {
        // 1. Получить логи CI через GitHub API
        val logs = githubMcp.getCILogs(prNumber)

        // 2. Проанализировать ошибку
        val analysis = analyzeError(logs)

        // 3. Исправить
        fixError(analysis)

        // 4. Коммит и push
        commitAndPush("fix: исправлена ошибка CI - ${analysis.summary}")

        // 5. Ждать новый CI
    }
}
```

### 2.2 10+ итераций review
```
⚠️ Выполнено 10 итераций review, но замечания остаются.

Нерешённые проблемы:
1. Потенциальная утечка памяти в logout() (строка 45)
2. Отсутствует обработка ошибок в API вызове

Варианты:
[1] Продолжить попытки исправить
[2] Замерджить как есть с пометкой "needs human review"
[3] Оставить PR открытым, завершить пайплайн

Выберите вариант:
```

### 2.3 Конфликты merge
- AI анализирует конфликт
- Принимает решение самостоятельно
- Резолвит конфликт и коммитит

### 2.4 Большие задачи (20+ файлов)
- Создаётся один большой PR
- Review проводится по частям
- Не разбивать на несколько PR

---

## 3. Безопасность

### 3.1 Защищённые файлы (нельзя редактировать)
```kotlin
val PROTECTED_PATTERNS = listOf(
    ".env",
    ".env.*",
    "**/secrets/**",
    "**/credentials/**",
    "**/*.pem",
    "**/*.key"
)
```

### 3.2 Остальные файлы
- Разрешено редактировать всё, кроме защищённых паттернов
- Включая: код, конфиги, build.gradle, yml файлы

---

## 4. Технические детали

### 4.1 Модели
| Этап | Модель | Причина |
|------|--------|---------|
| Анализ задачи | Claude Sonnet | Баланс цены/качества |
| Написание кода | Claude Sonnet | Хорошо пишет код |
| Self-Review | Claude Sonnet | Консистентность |
| Анализ CI логов | Claude Sonnet | Понимание контекста |

### 4.2 Состояние
- **Stateless**: каждый запуск независимый
- Не запоминает предыдущие запуски
- Не обучается на ошибках

### 4.3 Скоуп
- Только этот проект (zhebera/TestAppAiChallenge)
- Репозиторий захардкожен

### 4.4 Язык коммуникации
- **Консоль**: русский
- **Commit messages**: русский
- **PR description**: русский
- **Review comments**: русский

---

## 5. CI Workflow

Необходимо создать `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build
        run: ./gradlew build

      - name: Run tests
        run: ./gradlew test

      - name: Run lint (ktlint)
        run: ./gradlew ktlintCheck
```

---

## 6. Новые компоненты

### 6.1 FullCyclePipelineService
```kotlin
class FullCyclePipelineService(
    private val ragService: RagService,
    private val githubMcp: GitHubMcpClient,
    private val prReviewService: PrReviewService,
    private val llmClient: LlmClient,
    private val fileEditor: FileEditor
) {
    suspend fun execute(taskDescription: String): PipelineResult
}
```

### 6.2 FullCycleCommand
```kotlin
class FullCycleCommand : Command {
    override val name = "full-cycle"
    override val description = "Полный цикл: код → PR → review → merge"

    suspend fun execute(args: String)
}
```

### 6.3 PipelineState
```kotlin
sealed class PipelineState {
    object Analyzing : PipelineState()
    data class PlanReady(val plan: List<PlannedChange>) : PipelineState()
    object AwaitingConfirmation : PipelineState()
    object MakingChanges : PipelineState()
    data class CreatingPR(val branch: String) : PipelineState()
    data class Reviewing(val iteration: Int) : PipelineState()
    object WaitingForCI : PipelineState()
    data class FixingCIError(val error: String) : PipelineState()
    object Merging : PipelineState()
    data class Completed(val report: PipelineReport) : PipelineState()
    data class Failed(val reason: String) : PipelineState()
}
```

---

## 7. Интерфейс прогресса

Подробный лог каждого шага:

```
🔍 Анализирую задачу...
   Поиск релевантных файлов через RAG...
   Найдено 5 релевантных файлов

📋 План изменений:
   1. App.kt - добавить logout()
   2. SettingsScreen.kt - добавить кнопку

   Начать? [y/n] y

✏️ Вношу изменения...
   [1/2] Редактирую App.kt...
   [2/2] Редактирую SettingsScreen.kt...

🌿 Создаю ветку feature/ai-logout-1705123456...
   Коммит: feat: добавлена кнопка выхода в настройках
   Push в origin...

🔗 Создаю Pull Request...
   PR #123 создан: https://github.com/...

🔎 Self-review итерация 1...
   Найдено 2 замечания:
   - Отсутствует null-check в строке 45
   - Нет теста для edge case

✏️ Исправляю замечания...
   Коммит: fix: добавлен null-check и тест

🔎 Self-review итерация 2...
   Замечаний не найдено ✅

⏳ Ожидаю CI...
   Build: ✅
   Tests: ✅
   Lint: ✅

🔀 Мержу PR #123 в main...

✅ Готово! Отчёт выше.
```

---

## 8. Приоритет качества

- Тщательный анализ кодбейза через RAG
- Полный контекст при написании кода
- Детальный self-review
- Не торопиться, делать качественно

---

## 9. Ограничения

1. Работает только с этим проектом
2. Требует ANTHROPIC_API_KEY
3. Требует GITHUB_TOKEN с правами на push и merge
4. Stateless — не учится на предыдущих запусках
5. Максимум 10 итераций review перед запросом помощи
6. Не создаёт автоматический rollback

---

## 10. Будущие улучшения (out of scope)

- [ ] Поддержка любого репозитория по URL
- [ ] Память о предыдущих запусках
- [ ] Автоматический rollback при проблемах в production
- [ ] Интеграция с issue tracker (создание issues)
- [ ] Webhook-режим для автоматического запуска
- [ ] Разбиение больших задач на несколько PR

---

*Спецификация создана: 2026-01-18*
*Версия: 1.0*
